import styled from 'styled-components';

export const Ty1xs = styled.p<{ color: string }>`
  font-family: "Poppins", Arial, Helvetica, sans-serif;
  font-weight: 400;
  font-size: 0.75rem;
  line-height: 1.35;
  color: ${({color}) => color};
`;

export const Ty2xs = styled.p<{ color: string }>`
  font-family: "Roboto", Arial, Helvetica, sans-serif;
  font-weight: 400;
  font-size: 0.875rem;
  line-height: 1.35;
  color: ${({color}) => color};
`;

export const Ty1s = styled.p<{ color: string }>`
  font-family: "Poppins", Arial, Helvetica, sans-serif;
  font-weight: 400;
  font-size: 1rem;
  line-height: 1.5;
  color: ${({color}) => color};
`;

export const Ty2s = styled.p<{ color: string }>`
  font-family: "Roboto", Arial, Helvetica, sans-serif;
  font-weight: 400;
  font-size: 1rem;
  line-height: 1.5;
  color: ${({color}) => color};
`;

export const Ty1m = styled.p<{ color: string }>`
  font-family: "Poppins", Arial, Helvetica, sans-serif;
  font-weight: 500;
  font-size: 1.125rem;
  line-height: 1.35;
  color: ${({color}) => color};
`;

export const Ty1mb = styled.p<{ color: string }>`
  font-family: "Poppins", Arial, Helvetica, sans-serif;
  font-weight: 500;
  font-size: 1.125rem;
  line-height: 1.35;
  color: ${({color}) => color};
`;

export const Ty2m = styled.p<{ color: string }>`
  font-family: "Roboto", Arial, Helvetica, sans-serif;
  font-weight: 500;
  font-size: 1.125rem;
  line-height: 1.35;
  color: ${({color}) => color};
`;

export const Ty2mb = styled.p<{ color: string }>`
  font-family: "Roboto", Arial, Helvetica, sans-serif;
  font-weight: 500;
  font-size: 1.125rem;
  line-height: 1.35;
  color: ${({color}) => color};
`;

export const Ty1l = styled.p<{ color: string }>`
  font-family: "Poppins", Arial, Helvetica, sans-serif;
  font-weight: 400;
  font-size: 1.5rem;
  line-height: 1.35;
  color: ${({color}) => color};
`;

export const Ty2l = styled.p<{ color: string }>`
  font-family: "Roboto", Arial, Helvetica, sans-serif;
  font-weight: 400;
  font-size: 1.5rem;
  line-height: 1.5;
  color: ${({color}) => color};
`;

export const Ty2lb = styled.p<{ color: string }>`
  font-family: "Roboto", Arial, Helvetica, sans-serif;
  font-weight: 400;
  font-size: 1.5rem;
  line-height: 1.5;
  color: ${({color}) => color};
  letter-spacing: 0.015em;
  text-transform: uppercase;
`;

export const Ty1xl = styled.p<{ color: string }>`
  font-family: "Poppins", Arial, Helvetica, sans-serif;
  font-weight: 600;
  font-size: 2rem;
  line-height: 1.25;
  color: ${({color}) => color};
`;

export const Ty2xl = styled.p<{ color: string }>`
  font-family: "Roboto", Arial, Helvetica, sans-serif;
  font-weight: 600;
  font-size: 2rem;
  line-height: 1.25;
  color: ${({color}) => color};
`;

export const Ty1xxl = styled.p<{ color: string }>`
  font-family: "Poppins", Arial, Helvetica, sans-serif;
  font-weight: 600;
  font-size: 4rem;
  line-height: 1.125;
  color: ${({color}) => color};
`;
